general {
	colors = true
	color_good = "#2B2F40"
	color_degraded = "#2B2F40"
	color_bad = "#2B2F40"
	interval = 1
}

order += "battery all"
order += "tztime local"

battery all {
	format = "%status %percentage"
	# %remaining"
}

tztime local {
	format = "%Y-%m-%d %H:%M:%S"
}
